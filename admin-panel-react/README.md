# 🚀 Appointments Bot Admin Panel - React

Современная админ-панель для системы управления записями на приемы, построенная с использованием React 18+, TypeScript, Tailwind CSS v4, и shadcn/ui компонентов.

## ✨ Особенности

- **🎨 Современный дизайн** - Следует принципам Material Design 3
- **📱 Полностью responsive** - Оптимизировано для мобильных устройств и TWA
- **🔐 Безопасная аутентификация** - JWT токены с автоматическим обновлением
- **🌐 Многоязычность** - Поддержка EN/RU/HE
- **⚡ Быстрая работа** - Vite + React 18 + TypeScript
- **🎯 Готовые компоненты** - shadcn/ui + Radix UI

## 🏗️ Архитектура

```
admin-panel-react/
├── src/
│   ├── components/          # React компоненты
│   │   ├── ui/             # shadcn/ui компоненты
│   │   ├── Dashboard.tsx   # Главная страница
│   │   ├── LoginPage.tsx   # Страница входа
│   │   └── ...
│   ├── hooks/              # React хуки
│   │   └── useAuth.tsx     # Аутентификация
│   ├── services/           # API сервисы
│   │   └── api.ts          # Интеграция с бекендом
│   ├── styles/             # Стили
│   │   └── globals.css     # Глобальные стили
│   └── App.tsx             # Главный компонент
├── package.json
└── vite.config.ts
```

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+ 
- npm или yarn
- Запущенный бекенд на порту 4000

### Установка

```bash
# Перейти в папку проекта
cd admin-panel-react

# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev
```

Проект будет доступен по адресу: http://localhost:4200

### Сборка для продакшена

```bash
npm run build
```

## 🔐 Аутентификация

### Демо аккаунт
- **Email:** admin@system.com
- **Password:** admin123

### Регистрация нового аккаунта
1. Перейдите на вкладку "Register"
2. Заполните форму регистрации
3. Создайте новую организацию

## 📱 Мобильная версия

Админ-панель полностью адаптирована для мобильных устройств:
- **Responsive дизайн** - Адаптивная сетка и компоненты
- **Touch-friendly** - Удобные сенсорные элементы
- **TWA оптимизация** - Для Telegram Web App
- **Мобильные фильтры** - Drawer/Sheet компоненты

## 🎨 Дизайн система

### Цветовая палитра
```css
Primary:       #4F46E5  (indigo-600)
Primary Hover: #4338CA  (indigo-700)
Background:    #FAFAFA  (gray-50)
Card:          #FFFFFF  (white)
```

### Компоненты
- **Buttons** - Primary, Secondary, Ghost, Destructive
- **Forms** - Input, Select, Textarea с валидацией
- **Cards** - Статистика, действия, контент
- **Tables** - Адаптивные таблицы с мобильными карточками
- **Modals** - Dialog, Sheet, Drawer

## 🔌 API Интеграция

Проект интегрирован с существующим бекендом:

### Endpoints
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/register` - Регистрация
- `GET /api/appointments` - Список записей
- `GET /api/services` - Список услуг
- `GET /api/organizations` - Список организаций
- `GET /api/dashboard/stats` - Статистика дашборда

### Аутентификация
- JWT токены с автоматическим обновлением
- Сохранение сессии в localStorage
- Автоматическая проверка токена каждые 5 минут

## 📊 Страницы

### 🏠 Dashboard
- Общая статистика системы
- Быстрые действия
- Последние записи
- Графики и аналитика

### 📅 Appointments
- Управление записями
- Фильтрация по статусу, дате, услуге
- Мобильные карточки / Desktop таблицы
- Массовые операции

### 🔧 Services
- CRUD операции с услугами
- Многоязычные названия
- Настройка цен и длительности
- Управление доступностью

### 🏢 Organizations
- Управление организациями
- Настройка ботов
- Webhook конфигурация
- Мультитенантность

### 🤖 Bot Management
- Настройка Telegram ботов
- Тестирование webhook'ов
- Управление токенами
- Мониторинг активности

### ⏰ Slots
- Автоматическая генерация слотов
- Управление расписанием
- Настройка интервалов
- Массовые операции

### 🤖 AI Assistant
- Интеграция с OpenAI/Anthropic/Google AI
- Настройка промптов
- Тестирование ответов
- Управление провайдерами

### ⚙️ Settings
- Профиль пользователя
- Системные настройки
- Уведомления
- Безопасность

## 🛠️ Разработка

### Структура компонентов
```tsx
// Пример структуры страницы
export function PageName() {
  const { user } = useAuth();
  const [data, setData] = useState([]);
  const [isLoading, setIsLoading] = useState(true);

  const loadData = async () => {
    try {
      const response = await apiClient.getData();
      setData(response.data);
    } catch (error) {
      toast.error('Failed to load data');
    } finally {
      setIsLoading(false);
    }
  };

  useEffect(() => {
    loadData();
  }, []);

  return (
    <div className="flex-1 flex flex-col min-h-0 bg-gray-50">
      <PageHeader
        icon={<Icon className="w-7 h-7 text-white" />}
        title="Page Title"
        description="Page description"
        onRefresh={loadData}
      />
      
      <div className="flex-1 overflow-y-auto px-4 sm:px-6 py-6">
        <div className="max-w-7xl mx-auto space-y-6">
          {/* Content */}
        </div>
      </div>
    </div>
  );
}
```

### Стилизация
```tsx
// Используйте готовые классы Tailwind
<div className="bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-lg">
  Button
</div>

// Статусные цвета
<div className="bg-emerald-50 text-emerald-700 border-emerald-200">
  Success
</div>
```

### API вызовы
```tsx
// Используйте apiClient для всех запросов
const data = await apiClient.getAppointments({ limit: 10 });
const newService = await apiClient.createService(serviceData);
```

## 🧪 Тестирование

### Проверка компонентов
- [ ] Мобильная версия (320px - 1920px)
- [ ] Tablet view (768px - 1024px)  
- [ ] Desktop view (> 1024px)
- [ ] Формы и валидация
- [ ] Состояния загрузки
- [ ] Обработка ошибок
- [ ] Toast уведомления

### Тестирование API
```bash
# Проверить доступность бекенда
curl http://localhost:4000/api/health

# Тест аутентификации
curl -X POST http://localhost:4000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@system.com","password":"admin123"}'
```

## 🚀 Деплой

### Сборка
```bash
npm run build
```

### Настройка окружения
```bash
# .env.production
VITE_API_BASE_URL=https://your-api-domain.com/api
```

### Nginx конфигурация
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

## 📚 Документация

- [PROJECT.md](./src/PROJECT.md) - Полная документация проекта
- [STYLING_GUIDE.md](./src/STYLING_GUIDE.md) - Руководство по стилизации
- [CONSISTENCY_CHECKLIST.md](./src/CONSISTENCY_CHECKLIST.md) - Чек-лист консистентности

## 🤝 Вклад в проект

1. Следуйте установленным паттернам
2. Тестируйте на всех устройствах
3. Обновляйте документацию
4. Используйте TypeScript типы

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера
2. Убедитесь, что бекенд запущен
3. Проверьте сетевые запросы в DevTools
4. Обратитесь к документации

---

**Версия:** 1.0.0  
**Последнее обновление:** Октябрь 2025  
**Автор:** Development Team
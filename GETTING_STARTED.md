# 🚀 Getting Started - Appointments Bot

**Для нового агента:** Прочитай этот документ первым, чтобы быстро понять проект и начать работу.

## 📋 Что это за проект?

**Appointments Bot** - это полнофункциональная мультитенантная система бронирования записей с интеграцией Telegram бота и веб-админ панелью.

### Основные компоненты:
- 🔧 **Backend API** (Node.js + Express + Prisma) - порт 4000
- ⚛️ **React Admin Panel** - порт 4200
- 🌐 **Landing Page** (Next.js) - порт 3000
- 🤖 **Telegram Bot** - мультитенантный менеджер ботов
- 🗄️ **База данных** - SQLite (dev) / PostgreSQL (prod)
- 🔌 **WebSocket** - real-time обновления
- 🌍 **Multi-language** - русский, английский, иврит

## ⚡ Быстрый старт (5 минут)

### 1. Проверь зависимости
```bash
node --version  # Должно быть 20+
npm --version
```

### 2. Инициализируй базу данных
```bash
cd backend
npx prisma generate
npx prisma db push
```

### 3. Запусти все сервисы
```bash
# Из корня проекта
./start-all.sh

# Или вручную:
# Терминал 1: Backend
cd backend && npm run dev

# Терминал 2: React Admin
cd admin-panel-react && npm run dev

# Терминал 3: Landing
cd landing && npm run dev

# Терминал 4: Ngrok (для Telegram WebApp)
ngrok http 4000
```

### 4. Проверь что все работает
- Backend: http://localhost:4000/api/health → `{"ok":true}`
- React Admin: http://localhost:4200
- Landing: http://localhost:3000

## 🔐 Учетные данные

**Super Admin:**
- Email: `admin@system.com`
- Password: `admin123`

⚠️ **ВАЖНО:** Измени в production!

## 📁 Структура проекта

```
appointments-bot/
├── backend/              # Node.js API + Telegram Bot
│   ├── src/
│   │   ├── api/          # REST API routes
│   │   ├── bot/          # Telegram bot handlers
│   │   ├── websocket/    # WebSocket server
│   │   └── lib/          # Утилиты
│   └── prisma/           # Database schema
│
├── admin-panel-react/    # React Admin Panel
│   ├── src/
│   │   ├── components/   # React компоненты
│   │   ├── pages/        # Страницы
│   │   ├── services/     # API сервисы
│   │   └── i18n/         # Переводы (ru, en, he)
│
├── landing/              # Next.js Landing Page
│   ├── app/              # Next.js app directory
│   └── components/       # Компоненты лендинга
│
├── docs/                 # Документация
├── scripts/              # Скрипты автоматизации
└── docker-compose.yml    # Docker конфигурация
```

## 🎯 Ключевые файлы для изучения

1. **SYSTEM_PROMPT_FOR_GPT.md** - Полная архитектура системы (864 строки)
2. **AGENT_ONBOARDING_GUIDE.md** - Детальный гайд для агента
3. **README.md** - Основная документация
4. **QUICK_START.md** - Быстрый старт

## 🔧 Полезные команды

```bash
# Backend
cd backend
npm run dev              # Запуск dev сервера
npm run slots:month      # Генерация слотов на месяц
npm run create-system-admin  # Создание суперадмина
npx prisma studio        # GUI для базы данных

# React Admin
cd admin-panel-react
npm run dev              # Запуск dev сервера
npm run build            # Сборка для production

# Landing
cd landing
npm run dev              # Запуск dev сервера
```

## 🐛 Частые проблемы

### Порт занят
```bash
# Убить все процессы на портах
lsof -ti :3000 :4000 :4200 :4040 | xargs kill -9
```

### База данных не работает
```bash
cd backend
npx prisma db push
npx prisma generate
```

### Зависимости не установлены
```bash
cd backend && npm install
cd admin-panel-react && npm install
cd landing && npm install
```

## 📚 Документация

- **Архитектура:** `docs/architecture/`
- **API:** `docs/api/`
- **Deployment:** `docs/deployment/`
- **Development:** `docs/development/`

## 🚨 Критические моменты

1. **Telegram WebApp требует HTTPS** - используй ngrok для разработки
2. **База данных** - SQLite для dev, PostgreSQL для prod
3. **Мультитенантность** - каждая организация имеет своего бота
4. **Переводы** - все тексты в `admin-panel-react/src/i18n/`

## ✅ Чеклист для нового агента

- [ ] Прочитал `GETTING_STARTED.md` (этот файл)
- [ ] Прочитал `SYSTEM_PROMPT_FOR_GPT.md` (архитектура)
- [ ] Запустил все сервисы локально
- [ ] Проверил что все работает (health checks)
- [ ] Изучил структуру проекта
- [ ] Понял как работает мультитенантность
- [ ] Знаю где находятся переводы
- [ ] Знаю как работать с базой данных (Prisma)

---

**Готов к работе?** Начни с чтения `SYSTEM_PROMPT_FOR_GPT.md` для полного понимания архитектуры!


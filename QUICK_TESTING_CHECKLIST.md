# Быстрый чеклист тестирования

## 🚀 Быстрая проверка (5-10 минут)

### Вход
- [ ] Войти: `some@test.com` / `Test1234`

### Dashboard (2 минуты)
- [ ] AppointmentsSummaryCard отображается с данными
- [ ] Bot Status Alerts показывают правильные статусы
- [ ] Welcome секция отображается

### AppointmentsPage (2 минуты)
- [ ] Статистика Rejected отображается
- [ ] Вкладка "Rejected" работает в tabs
- [ ] Фильтрация по Rejected работает

### AnalyticsPage (1 минута)
- [ ] Графики отображаются (LineChart и BarChart)

### BotManagementPage (1 минута)
- [ ] Empty State отображается если бот не настроен

### NotificationCenter (1 минута)
- [ ] Tabs (All/Unread) работают
- [ ] Группировка по датам работает

### Backend API (1 минута)
- [ ] `/api/appointments/summary-stats` возвращает данные
- [ ] `/api/bot/status/:organizationId` возвращает `botActive` и `adminLinked`

---

## ✅ Критические проверки

### Обязательно проверить:
1. **Dashboard не падает** при загрузке
2. **AppointmentsSummaryCard отображает корректные данные**
3. **Фильтр Rejected работает** на AppointmentsPage
4. **Графики отображаются** на AnalyticsPage
5. **Bot Status Alerts показывают правильные статусы**
6. **Empty State отображается** на BotManagementPage
7. **NotificationCenter работает** с новыми tabs

---

## 🐛 Если что-то не работает

1. Проверить консоль браузера (F12) на ошибки
2. Проверить Network tab - все запросы возвращают 200 OK?
3. Проверить backend логи
4. Проверить, что все сервисы запущены

---

## 📝 Быстрые команды для проверки

### В консоли браузера:
```javascript
// Проверить API клиент
window.apiClient.getAppointmentsSummaryStats().then(console.log);

// Проверить Bot Status
window.apiClient.getBotStatus(1).then(console.log);
```

### Проверить endpoints напрямую:
```bash
# Appointments Summary Stats
curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost:3000/api/appointments/summary-stats

# Bot Status
curl -H "Authorization: Bearer YOUR_TOKEN" http://localhost:3000/api/bot/status/1
```


# 🚀 Запуск всех сервисов - Appointments Bot

**Цель:** Запустить все сервисы для полного тестирования

---

## 📋 Чеклист сервисов

- [ ] Landing page (порт 3000)
- [ ] React Admin Panel (порт 4200)
- [ ] Backend API (порт 4000)
- [ ] База данных (SQLite)
- [ ] Telegram WebApp (готов к тестированию)

---

## 🚀 Пошаговая инструкция

### Шаг 1: Проверить базу данных

```bash
cd backend
npx prisma db push
npm run seed
```

**Ожидаемый результат:** База данных создана, тестовые данные загружены

---

### Шаг 2: Запустить Backend (порт 4000)

```bash
cd backend
npm run dev
```

**Ожидаемый результат:** 
- Backend запущен на http://localhost:4000
- В логах: `API on http://127.0.0.1:4000`
- Health check: `curl http://localhost:4000/api/health` → `{"ok":true}`

**Оставить запущенным в отдельном терминале!**

---

### Шаг 3: Запустить React Admin Panel (порт 4200)

```bash
cd admin-panel-react
npm run dev
```

**Ожидаемый результат:**
- React панель запущена на http://localhost:4200
- Страница загружается в браузере

**Оставить запущенным в отдельном терминале!**

---

### Шаг 4: Запустить Landing Page (порт 3000)

```bash
cd landing
npm install  # если зависимости не установлены
npm run dev
```

**Ожидаемый результат:**
- Landing запущен на http://localhost:3000
- Страница загружается в браузере

**Оставить запущенным в отдельном терминале!**

---

### Шаг 5: Собрать React панель для Telegram WebApp

```bash
cd admin-panel-react
npm run build
```

**Ожидаемый результат:**
- Создана папка `build/` с собранной панелью
- Backend автоматически найдет и будет отдавать эту панель для `/admin-panel`

---

### Шаг 6: Проверить Telegram WebApp

1. **URL для WebApp:** `http://localhost:4000/webapp/admin`
2. **Или через бота:** Отправить команду `/admin` в Telegram боте
3. **Ожидаемый результат:**
   - WebApp открывается
   - Redirect на `/admin-panel`
   - React панель загружается

---

## ✅ Финальная проверка

### 1. Проверить все порты:
```bash
lsof -i :3000 -i :4000 -i :4200
```

**Ожидаемый результат:** Все три порта заняты процессами

### 2. Проверить health checks:
```bash
# Backend
curl http://localhost:4000/api/health
# Ожидается: {"ok":true}

# Frontend (React)
curl http://localhost:4200
# Ожидается: HTML страница

# Landing
curl http://localhost:3000
# Ожидается: HTML страница
```

### 3. Проверить Telegram WebApp:
- Открыть в браузере: `http://localhost:4000/webapp/admin`
- Должен быть redirect на `/admin-panel`
- React панель должна загрузиться

---

## 🐛 Troubleshooting

### Backend не запускается
- Проверить что порт 4000 свободен
- Проверить .env файл
- Проверить базу данных: `npx prisma db push`

### Frontend не запускается
- Проверить что порт 4200 свободен
- Установить зависимости: `npm install`
- Проверить node_modules

### Landing не запускается
- Проверить что порт 3000 свободен
- Установить зависимости: `npm install`
- Проверить Next.js версию

### Telegram WebApp не работает
- Убедиться что React панель собрана: `npm run build` в admin-panel-react
- Проверить что backend запущен
- Проверить URL: `http://localhost:4000/webapp/admin`

---

## 📝 Примечания

- **База данных:** SQLite (не нужен Docker для dev)
- **Backend:** Автоматически отдает React build для `/admin-panel`
- **Telegram WebApp:** Использует same-origin для избежания CORS проблем
- **Все сервисы:** Запускаются в режиме разработки (hot reload)

---

**Готово! Все сервисы должны быть запущены! 🎉**


# 👥 Руководство по совместной работе в команде

> **Для команды из 2 разработчиков** работающих в Cursor на одном репозитории

## 📋 Содержание

1. [Git Workflow](#git-workflow)
2. [Разделение ответственности](#разделение-ответственности)
3. [Процессы синхронизации](#процессы-синхронизации)
4. [Конвенции коммитов](#конвенции-коммитов)
5. [Code Review](#code-review)
6. [Работа в Cursor](#работа-в-cursor)
7. [Решение конфликтов](#решение-конфликтов)
8. [Ежедневные практики](#ежедневные-практики)

---

## 🔀 Git Workflow

### Основная стратегия: **Feature Branch Workflow**

```
main (production-ready)
  ├── develop (integration branch)
  │   ├── feature/user-auth
  │   ├── feature/booking-flow
  │   └── bugfix/websocket-connection
```

### Правила работы с ветками:

1. **`main`** - только стабильный, протестированный код
   - Защищена от прямых коммитов
   - Только через Pull Request
   - Требует code review

2. **`develop`** - ветка для интеграции
   - Основная рабочая ветка
   - Все feature ветки мержатся сюда
   - Периодически мержится в `main`

3. **Feature ветки** - для новой функциональности
   - Формат: `feature/краткое-описание`
   - Примеры: `feature/telegram-webapp`, `feature/ai-assistant`

4. **Bugfix ветки** - для исправления багов
   - Формат: `bugfix/краткое-описание`
   - Примеры: `bugfix/auth-token-expiry`, `bugfix/slot-booking`

5. **Hotfix ветки** - для критических исправлений
   - Формат: `hotfix/краткое-описание`
   - Мержатся сразу в `main` и `develop`

### Типичный workflow:

```bash
# 1. Начать новую задачу
git checkout develop
git pull origin develop
git checkout -b feature/my-new-feature

# 2. Работа над задачей
# ... делаем изменения ...

# 3. Коммитим изменения
git add .
git commit -m "feat: add user authentication"

# 4. Пуш в удаленный репозиторий
git push origin feature/my-new-feature

# 5. Создаем Pull Request в develop
# (через GitHub/GitLab UI)

# 6. После мержа - удаляем локальную ветку
git checkout develop
git pull origin develop
git branch -d feature/my-new-feature
```

---

## 🎯 Разделение ответственности

### Рекомендуемое разделение по компонентам:

#### Разработчик 1 (Backend Focus):
- ✅ Backend API (`backend/src/api/`)
- ✅ Telegram Bot (`backend/src/bot/`)
- ✅ WebSocket сервер (`backend/src/websocket/`)
- ✅ Database schema (`backend/prisma/`)
- ✅ Backend утилиты (`backend/src/lib/`)

#### Разработчик 2 (Frontend Focus):
- ✅ React Admin Panel (`admin-panel-react/src/`)
- ✅ Landing Page (`landing/`)
- ✅ UI компоненты и стили
- ✅ Frontend интеграции

### Общие зоны (требуют координации):
- 🔄 API контракты (backend ↔ frontend)
- 🔄 Database migrations
- 🔄 Environment variables
- 🔄 Docker конфигурация
- 🔄 Документация

### Правила работы в общих зонах:

1. **API изменения:**
   - Сначала обсудить в чате/комментариях
   - Backend разработчик создает endpoint
   - Frontend разработчик интегрирует
   - Обновить API документацию

2. **Database migrations:**
   - Создавать через Prisma: `npx prisma migrate dev`
   - Коммитить миграции в отдельном PR
   - Уведомлять партнера о миграциях

3. **Breaking changes:**
   - Обязательно обсудить заранее
   - Создать Issue с описанием
   - Дать время на адаптацию

---

## 🔄 Процессы синхронизации

### Ежедневная синхронизация:

#### Утром (перед началом работы):
```bash
# 1. Обновить develop
git checkout develop
git pull origin develop

# 2. Обновить свою feature ветку
git checkout feature/my-feature
git rebase develop  # или git merge develop

# 3. Проверить статус
git status
```

#### В течение дня:
- Коммитить часто (каждые 1-2 часа работы)
- Пуш в удаленный репозиторий минимум 2 раза в день
- Обновляться с develop минимум раз в день

#### Вечером (перед завершением):
```bash
# 1. Закоммитить все изменения
git add .
git commit -m "..."

# 2. Запушить в удаленный репозиторий
git push origin feature/my-feature

# 3. Создать/обновить Pull Request если готово
```

### Еженедельная синхронизация:

- **Понедельник:** Планирование задач на неделю
- **Среда:** Code review сессия
- **Пятница:** Мерж develop → main (если все готово)

---

## 📝 Конвенции коммитов

### Формат: `type(scope): subject`

```
feat(backend): add user authentication endpoint
fix(frontend): resolve calendar date picker bug
docs(readme): update installation instructions
refactor(websocket): improve connection handling
test(api): add tests for appointments endpoint
chore(deps): update dependencies
```

### Типы коммитов:

- **`feat`** - новая функциональность
- **`fix`** - исправление бага
- **`docs`** - изменения в документации
- **`style`** - форматирование, отсутствующие точки с запятой и т.д.
- **`refactor`** - рефакторинг кода
- **`test`** - добавление тестов
- **`chore`** - обновление зависимостей, конфигурации и т.д.
- **`perf`** - улучшение производительности
- **`ci`** - изменения CI/CD

### Scope (область изменений):

- `backend` - изменения в backend
- `frontend` - изменения в React Admin Panel
- `landing` - изменения в Landing Page
- `bot` - изменения в Telegram Bot
- `websocket` - изменения в WebSocket
- `database` - изменения в схеме БД
- `docs` - документация
- `deps` - зависимости

### Примеры хороших коммитов:

```bash
feat(backend): add appointment cancellation endpoint
fix(frontend): resolve timezone issue in calendar
docs(api): document new authentication flow
refactor(bot): simplify booking handler logic
test(backend): add tests for slot generation
chore(deps): update prisma to 6.17.1
```

### Примеры плохих коммитов:

```bash
# ❌ Плохо
git commit -m "fix"
git commit -m "update"
git commit -m "changes"
git commit -m "WIP"
git commit -m "asdf"

# ✅ Хорошо
git commit -m "fix(frontend): resolve calendar date picker bug"
git commit -m "feat(backend): add user authentication endpoint"
```

---

## 👀 Code Review

### Процесс:

1. **Создание PR:**
   - Заполнить шаблон Pull Request
   - Добавить описание изменений
   - Указать связанные Issues
   - Добавить скриншоты (если UI изменения)

2. **Review процесс:**
   - Партнер проверяет код
   - Оставляет комментарии
   - Запрашивает изменения если нужно
   - Одобряет когда готово

3. **После одобрения:**
   - Автор PR мержит изменения
   - Удаляет feature ветку
   - Обновляет документацию если нужно

### Чеклист для Review:

- [ ] Код работает как ожидается?
- [ ] Нет очевидных багов?
- [ ] Код читаемый и понятный?
- [ ] Следует конвенциям проекта?
- [ ] Нет дублирования кода?
- [ ] Добавлены комментарии где нужно?
- [ ] Обновлена документация?
- [ ] Нет security issues?
- [ ] Производительность приемлема?

### Правила Review:

- **Быть конструктивным** - критиковать код, а не человека
- **Быть конкретным** - указывать точные места и предложения
- **Быть вежливым** - использовать дружелюбный тон
- **Быть быстрым** - отвечать в течение 24 часов
- **Быть открытым** - принимать критику и предложения

---

## 💻 Работа в Cursor

### Best Practices для команды:

#### 1. **Использование Cursor Rules:**

Создайте файл `.cursorrules` в корне проекта:

```markdown
# Cursor Rules для Appointments Bot

## Технологический стек
- Backend: Node.js 20, Express, Prisma, TypeScript
- Frontend: React 18, TypeScript, Tailwind CSS
- Database: SQLite (dev) / PostgreSQL (prod)

## Стиль кода
- Используй TypeScript strict mode
- Следуй существующим паттернам в проекте
- Используй async/await вместо promises
- Добавляй JSDoc комментарии для сложных функций

## Git
- Используй conventional commits
- Создавай feature ветки для новых задач
- Коммить часто, но осмысленно

## Тестирование
- Покрывай критический функционал тестами
- Используй descriptive test names
```

#### 2. **Синхронизация через Git:**

- Cursor автоматически отслеживает изменения в Git
- Используйте встроенный Git UI в Cursor
- Регулярно делайте `git pull` перед началом работы

#### 3. **Избегайте конфликтов:**

- Работайте в разных файлах когда возможно
- Общайтесь о больших изменениях заранее
- Используйте `.cursorignore` для исключения файлов

#### 4. **Использование AI в Cursor:**

- Используйте AI для code review перед коммитом
- Просите AI объяснить сложные части кода
- Используйте AI для генерации тестов
- НО: всегда проверяйте AI код перед коммитом

---

## 🔧 Решение конфликтов

### Предотвращение конфликтов:

1. **Частые синхронизации:**
   ```bash
   git pull origin develop  # минимум раз в день
   ```

2. **Работа в разных файлах:**
   - Координируйте кто над чем работает
   - Используйте Issues для планирования

3. **Мелкие коммиты:**
   - Коммитьте часто
   - Один коммит = одна логическая единица

### Решение конфликтов:

#### Если конфликт при merge:

```bash
# 1. Обновить develop
git checkout develop
git pull origin develop

# 2. Вернуться в feature ветку
git checkout feature/my-feature

# 3. Попробовать merge
git merge develop

# 4. Если конфликт - разрешить вручную
# Cursor покажет конфликтные участки

# 5. После разрешения
git add .
git commit -m "merge: resolve conflicts with develop"

# 6. Пуш
git push origin feature/my-feature
```

#### Стратегии разрешения:

1. **Accept Current Change** - оставить свою версию
2. **Accept Incoming Change** - взять версию из develop
3. **Accept Both Changes** - объединить обе версии
4. **Manual Edit** - вручную отредактировать

#### Если конфликт сложный:

- Обсудить с партнером
- Возможно использовать `git rebase` вместо `merge`
- Создать отдельную ветку для разрешения конфликта

---

## 📅 Ежедневные практики

### Утренний ритуал:

1. ✅ Обновить код: `git pull origin develop`
2. ✅ Проверить Issues и PR
3. ✅ Обновить статус задач
4. ✅ Синхронизироваться с партнером (если нужно)

### В течение дня:

1. ✅ Коммитить часто (каждые 1-2 часа)
2. ✅ Пуш в удаленный репозиторий минимум 2 раза
3. ✅ Обновляться с develop минимум раз в день
4. ✅ Общаться о блокерах и вопросах

### Вечерний ритуал:

1. ✅ Закоммитить все изменения
2. ✅ Запушить в удаленный репозиторий
3. ✅ Обновить статус задач
4. ✅ Создать/обновить PR если готово

### Еженедельные практики:

- **Понедельник:** Планирование недели, обсуждение приоритетов
- **Среда:** Code review сессия, обсуждение архитектуры
- **Пятница:** Подведение итогов, мерж в main если готово

---

## 🚨 Важные правила

### ❌ НИКОГДА:

- Не коммитить в `main` напрямую
- Не форсить пуш в `main` или `develop`
- Не мержить свой собственный PR без review
- Не коммитить `.env` файлы
- Не коммитить `node_modules`
- Не коммитить секреты и токены
- Не удалять чужие ветки без разрешения

### ✅ ВСЕГДА:

- Создавать feature ветки для новых задач
- Писать понятные commit messages
- Обновляться с develop регулярно
- Делать code review перед мержем
- Обновлять документацию при изменениях
- Тестировать код перед коммитом
- Общаться о больших изменениях

---

## 📞 Коммуникация

### Каналы общения:

1. **GitHub/GitLab Issues** - для задач и багов
2. **Pull Request комментарии** - для code review
3. **Telegram/Discord** - для быстрой синхронизации
4. **Документация** - для важных решений

### Что обсуждать:

- ✅ Большие архитектурные изменения
- ✅ Breaking changes в API
- ✅ Database migrations
- ✅ Новые зависимости
- ✅ Блокеры и проблемы
- ✅ Приоритеты задач

### Что НЕ нужно обсуждать:

- ❌ Мелкие багфиксы
- ❌ Рефакторинг без изменения API
- ❌ Обновление документации
- ❌ Добавление тестов

---

## 📊 Метрики успеха

### Отслеживайте:

- Количество конфликтов (должно уменьшаться)
- Время на code review (цель: < 24 часа)
- Частота коммитов (цель: регулярно)
- Качество commit messages
- Покрытие тестами

---

## 🎯 Быстрый чеклист

### Перед началом работы:
- [ ] `git pull origin develop`
- [ ] Проверить Issues
- [ ] Синхронизироваться с партнером

### Во время работы:
- [ ] Работать в feature ветке
- [ ] Коммитить часто
- [ ] Пуш минимум 2 раза в день
- [ ] Обновляться с develop раз в день

### Перед коммитом:
- [ ] Код работает?
- [ ] Нет очевидных багов?
- [ ] Commit message понятный?
- [ ] Нет секретов в коде?

### Перед PR:
- [ ] Все тесты проходят?
- [ ] Документация обновлена?
- [ ] PR описание заполнено?
- [ ] Код готов к review?

---

**Последнее обновление:** 2025-01-18  
**Версия:** 1.0.0


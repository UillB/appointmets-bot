# Cursor Rules для Appointments Bot

## 🎯 Контекст проекта

Это мультитенантная система бронирования записей с Telegram ботом и веб-админ панелью.

## 🏗️ Технологический стек

### Backend
- **Runtime:** Node.js 20+
- **Framework:** Express.js 5.1.0
- **ORM:** Prisma 6.17.1
- **Database:** SQLite (dev) / PostgreSQL (prod)
- **Telegram Bot:** Telegraf.js 4.16.3
- **WebSocket:** ws 8.18.3 + Socket.io 4.8.1
- **Language:** TypeScript (strict mode)

### Frontend (Admin Panel)
- **Framework:** React 18.3.1 + TypeScript
- **Build Tool:** Vite 6.3.5
- **UI Library:** Radix UI компоненты
- **Styling:** Tailwind CSS 4.0.0
- **Routing:** React Router DOM 7.9.4
- **Forms:** React Hook Form 7.55.0

### Frontend (Landing)
- **Framework:** Next.js 14
- **Styling:** Tailwind CSS
- **i18n:** Встроенная поддержка многоязычности

## 📝 Стиль кода

### TypeScript
- Используй strict mode
- Избегай `any` типов
- Используй интерфейсы для объектов
- Добавляй JSDoc комментарии для публичных функций
- Используй `async/await` вместо promises

### Naming Conventions
- **Функции:** camelCase, глаголы (например: `getUserById`, `createAppointment`)
- **Компоненты:** PascalCase (например: `AppointmentCard`, `UserProfile`)
- **Константы:** UPPER_SNAKE_CASE (например: `MAX_RETRY_COUNT`)
- **Типы/Интерфейсы:** PascalCase (например: `AppointmentStatus`, `UserRole`)
- **Файлы:** kebab-case для компонентов, camelCase для утилит

### Code Organization
- Один компонент/функция = один файл
- Группируй связанные функции в модули
- Используй barrel exports (`index.ts`) для публичных API
- Разделяй логику и представление

## 🔄 Git Workflow

### Branch Strategy
- `main` - production-ready код
- `develop` - integration branch
- `feature/название` - новые фичи
- `bugfix/название` - исправления багов
- `hotfix/название` - критические исправления

### Commit Messages
Используй conventional commits:
```
type(scope): subject

feat(backend): add user authentication endpoint
fix(frontend): resolve calendar date picker bug
docs(readme): update installation instructions
refactor(websocket): improve connection handling
```

**Типы:** feat, fix, docs, style, refactor, test, chore, perf, ci

## 🗄️ Database (Prisma)

- Всегда используй Prisma Client для работы с БД
- Создавай миграции через `npx prisma migrate dev`
- Не редактируй миграции вручную после создания
- Используй транзакции для связанных операций
- Добавляй индексы для часто используемых полей

## 🔐 Безопасность

- Никогда не коммить `.env` файлы
- Храни секреты в environment variables
- Валидируй все входные данные (используй Zod)
- Используй JWT для аутентификации
- Проверяй права доступа (роли пользователей)
- Защищай от SQL injection (Prisma делает это автоматически)

## 🌍 Мультиязычность

- Все тексты должны быть в файлах переводов
- Используй ключи из `i18n/` файлов
- Не хардкодь тексты в компонентах
- Поддерживай: ru, en, he

## 🧪 Тестирование

- Покрывай критический функционал тестами
- Используй descriptive test names
- Тестируй edge cases
- Мокируй внешние зависимости

## 📚 Документация

- Обновляй README при изменениях
- Документируй публичные API
- Добавляй комментарии к сложной логике
- Обновляй архитектурную документацию

## 🎨 UI/UX Guidelines

- Используй компоненты из Radix UI
- Следуй дизайн-системе проекта
- Поддерживай темную тему
- Делай UI responsive
- Используй Tailwind CSS для стилей
- Добавляй loading states
- Обрабатывай ошибки gracefully

## ⚡ Производительность

- Используй lazy loading для компонентов
- Оптимизируй database queries
- Кэшируй часто используемые данные
- Минимизируй bundle size
- Используй WebSocket для real-time обновлений

## 🐛 Error Handling

- Всегда обрабатывай ошибки
- Используй try/catch для async операций
- Логируй ошибки (но не секреты)
- Показывай понятные сообщения пользователю
- Используй error boundaries в React

## 🔌 API Design

- Используй RESTful conventions
- Возвращай консистентные форматы ответов
- Используй HTTP статус коды правильно
- Валидируй входные данные
- Документируй endpoints

## 🤖 Telegram Bot

- Обрабатывай все команды
- Используй inline кнопки для UX
- Поддерживай мультиязычность
- Логируй важные события
- Обрабатывай ошибки gracefully

## 📦 Dependencies

- Обновляй зависимости регулярно
- Проверяй security vulnerabilities
- Не добавляй зависимости без необходимости
- Используй exact versions в production

## 🚨 Важные правила

- НЕ коммить `.env` файлы
- НЕ коммить `node_modules`
- НЕ коммить секреты и токены
- НЕ форсить пуш в `main` или `develop`
- ВСЕГДА создавай feature ветки для новых задач
- ВСЕГДА делай code review перед мержем
- ВСЕГДА обновляйся с develop перед началом работы

## 🚫 Запрещенные команды (требуют явного разрешения)

- **НЕ выполнять `git commit`** без явного запроса пользователя
- **НЕ выполнять `git push`** без явного запроса пользователя
- Эти команды должны выполняться только когда пользователь явно просит: "commit", "push", "commit and push" и т.д.
- Если нужно закоммитить или запушить изменения, сначала спроси пользователя или дождись явного запроса

## 💡 Best Practices

- Код должен быть читаемым и понятным
- Следуй принципу DRY (Don't Repeat Yourself)
- Используй TypeScript для type safety
- Рефакторь код регулярно
- Общайся с командой о больших изменениях
- Тестируй код перед коммитом


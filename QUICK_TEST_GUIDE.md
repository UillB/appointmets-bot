# 🚀 Быстрый план тестирования - Appointments Bot

**Дата создания:** 2025-11-04  
**Цель:** Убедиться что все работает перед началом разработки

---

## ✅ Шаг 0: Проверка текущего состояния

**Что уже работает:**
- ✅ Backend запущен на порту 4000
- ✅ Frontend (React) запущен на порту 4200
- ✅ База данных SQLite настроена
- ✅ Пользователь `admin@system.com` создан

**Текущие процессы:**
- Backend: запущен (процесс 23714)
- Frontend: запущен (процесс 23727)

---

## 📋 Шаг 1: Проверка Backend API

### 1.1 Health Check
```bash
curl http://localhost:4000/api/health
```
**Ожидаемый результат:** `{"ok":true}`

### 1.2 Тест Login API
```bash
curl -X POST http://localhost:4000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@system.com","password":"admin123"}'
```
**Ожидаемый результат:** 
- Статус 200 OK
- JSON с `accessToken` и `refreshToken`
- Информация о пользователе

**Сохраните токен для следующих тестов:**
```bash
TOKEN="ваш_токен_здесь"
```

---

## 🖥️ Шаг 2: Тестирование React Admin Panel

### 2.1 Открыть Frontend
1. Откройте браузер
2. Перейдите на: **http://localhost:4200**
3. **Ожидаемый результат:** Страница загружается, форма логина видна

### 2.2 Тест Login через UI
1. Введите:
   - **Email:** `admin@system.com`
   - **Password:** `admin123`
2. Нажмите "Login" или "Войти"
3. **Ожидаемый результат:** 
   - Успешный вход
   - Редирект на dashboard
   - Меню отображается

### 2.3 Проверка Dashboard
После входа проверьте:
- [ ] Dashboard загружается
- [ ] Видны статистики (appointments, services, organizations)
- [ ] Нет ошибок в консоли браузера (F12 → Console)

---

## 📊 Шаг 3: Тестирование основных функций

### 3.1 Organizations (Организации)
1. Перейдите в раздел "Organizations" или "Организации"
2. **Проверьте:**
   - [ ] Список организаций отображается
   - [ ] Можно создать новую организацию (кнопка "Create" или "+")
   - [ ] Можно редактировать существующую организацию
   - [ ] Можно удалить организацию

**Тест создания:**
- Нажмите "Create" или "+"
- Заполните форму:
  - Name: `Test Organization`
  - Description: `Test Description` (опционально)
- Нажмите "Save" или "Сохранить"
- **Ожидаемый результат:** Организация создана и видна в списке

### 3.2 Services (Сервисы)
1. Перейдите в раздел "Services" или "Сервисы"
2. **Проверьте:**
   - [ ] Список сервисов отображается
   - [ ] Можно создать новый сервис
   - [ ] Можно редактировать сервис
   - [ ] Можно удалить сервис

**Тест создания:**
- Нажмите "Create" или "+"
- Заполните форму:
  - Name (RU): `Консультация`
  - Name (EN): `Consultation`
  - Name (HE): `ייעוץ`
  - Duration: `30` минут
  - Price: `1000` (опционально)
  - Organization: выберите организацию
- Нажмите "Save"
- **Ожидаемый результат:** Сервис создан

### 3.3 Appointments (Назначения)
1. Перейдите в раздел "Appointments" или "Назначения"
2. **Проверьте:**
   - [ ] Список appointments отображается (может быть пустым)
   - [ ] Можно создать новое appointment
   - [ ] Можно редактировать appointment
   - [ ] Можно удалить appointment

### 3.4 Slots (Слоты)
1. Перейдите в раздел "Slots" или "Слоты"
2. **Проверьте:**
   - [ ] Список слотов отображается
   - [ ] Можно создать новые слоты
   - [ ] Можно редактировать слот
   - [ ] Можно удалить слот

---

## 🔌 Шаг 4: Дополнительное тестирование API

### 4.1 Получить организации (с токеном)
```bash
TOKEN="ваш_токен_из_шага_1.2"
curl http://localhost:4000/api/organizations \
  -H "Authorization: Bearer $TOKEN"
```
**Ожидаемый результат:** JSON массив с организациями

### 4.2 Получить сервисы
```bash
curl http://localhost:4000/api/services \
  -H "Authorization: Bearer $TOKEN"
```
**Ожидаемый результат:** JSON массив с сервисами

### 4.3 Получить appointments
```bash
curl http://localhost:4000/api/appointments \
  -H "Authorization: Bearer $TOKEN"
```
**Ожидаемый результат:** JSON массив с appointments

### 4.4 Получить слоты
```bash
curl http://localhost:4000/api/slots \
  -H "Authorization: Bearer $TOKEN"
```
**Ожидаемый результат:** JSON массив со слотами

---

## 🤖 Шаг 5: Тестирование Telegram Bot (опционально)

### 5.1 Проверить статус бота
```bash
curl http://localhost:4000/api/bot/status \
  -H "Authorization: Bearer $TOKEN"
```

**Если есть токен бота:**
1. Найдите бота в Telegram
2. Отправьте `/start`
3. Проверьте ответ бота
4. Протестируйте команды:
   - `/help` - помощь
   - `/book` - бронирование
   - `/my` - мои назначения
   - `/slots` - доступные слоты
   - `/lang` - смена языка

---

## ✅ Финальный чеклист

Перед тем как считать тестирование завершенным, убедитесь:

### Backend
- [ ] `/api/health` возвращает `{"ok":true}`
- [ ] `/api/auth/login` работает (возвращает токен)
- [ ] Все основные API endpoints работают с токеном
- [ ] Нет ошибок в логах backend

### Frontend
- [ ] Страница http://localhost:4200 загружается
- [ ] Логин работает (можно войти)
- [ ] Dashboard отображается после входа
- [ ] Все основные разделы работают:
  - [ ] Organizations
  - [ ] Services
  - [ ] Appointments
  - [ ] Slots
- [ ] Можно создавать/редактировать/удалять данные
- [ ] Нет критических ошибок в консоли браузера

### Общее
- [ ] Система готова к разработке
- [ ] Все основные функции работают
- [ ] Нет критических багов

---

## 🐛 Если что-то не работает

### Backend не отвечает
```bash
# Проверить процесс
ps aux | grep "tsx watch src/server.ts"

# Перезапустить backend
cd backend
npm run dev
```

### Frontend не отвечает
```bash
# Проверить процесс
ps aux | grep "vite"

# Перезапустить frontend
cd admin-panel-react
npm run dev
```

### Login не работает
```bash
# Создать пользователя заново
cd backend
npm run create-system-admin
```

### База данных проблемы
```bash
cd backend
npx prisma db push
npm run seed
```

---

## 📝 Заметки

**Текущие URL:**
- Backend API: http://localhost:4000
- React Admin Panel: http://localhost:4200
- Prisma Studio: http://localhost:5555 (запустить: `cd backend && npx prisma studio`)

**Учетные данные:**
- Email: `admin@system.com`
- Password: `admin123`
- Role: `SUPER_ADMIN`

---

**Готово к тестированию! 🎉**

Если все работает - можно начинать разработку. Если есть проблемы - сообщите мне, и я помогу их решить.


# ✅ Быстрый чеклист запуска всех сервисов

**Цель:** Запустить все сервисы для тестирования

---

## 🚀 Быстрый запуск (автоматический)

```bash
# Из корня проекта
./start-all.sh
```

Этот скрипт автоматически:
1. ✅ Настроит базу данных
2. ✅ Запустит Backend (4000)
3. ✅ Запустит React Admin (4200)
4. ✅ Запустит Landing (3000)
5. ✅ Соберет React панель для WebApp

---

## 📋 Ручной запуск (пошагово)

### 1. База данных ✅
```bash
cd backend
npx prisma db push
npm run create-system-admin
```
**Результат:** База готова, пользователь `admin@system.com` создан

---

### 2. Backend (порт 4000) ✅
```bash
cd backend
npm run dev
```
**Оставить в отдельном терминале!**

**Проверка:**
```bash
curl http://localhost:4000/api/health
# Ожидается: {"ok":true}
```

---

### 3. React Admin Panel (порт 4200) ✅
```bash
cd admin-panel-react
npm run dev
```
**Оставить в отдельном терминале!**

**Проверка:** Открыть http://localhost:4200 в браузере

---

### 4. Landing Page (порт 3000) ✅
```bash
cd landing
npm install  # если нужно
npm run dev
```
**Оставить в отдельном терминале!**

**Проверка:** Открыть http://localhost:3000 в браузере

---

### 5. Сборка React для Telegram WebApp ✅
```bash
cd admin-panel-react
npm run build
```

**Результат:** Создана папка `build/` - Backend автоматически отдаст её для `/admin-panel`

---

## 🌐 URL для тестирования

| Сервис | URL | Статус |
|--------|-----|--------|
| **Backend API** | http://localhost:4000 | ✅ |
| **React Admin** | http://localhost:4200 | ✅ |
| **Landing** | http://localhost:3000 | ✅ |
| **Telegram WebApp** | http://localhost:4000/webapp/admin | ✅ |

---

## 🤖 Telegram WebApp - Как протестировать

### Вариант 1: Через браузер (для тестирования)
1. Откройте: `http://localhost:4000/webapp/admin`
2. Должен быть redirect на `/admin-panel`
3. React панель должна загрузиться

### Вариант 2: Через Telegram бота
1. Найдите своего бота в Telegram
2. Отправьте команду `/admin`
3. Нажмите на кнопку "🔧 Открыть панель"
4. WebApp откроется с React панелью

---

## ✅ Финальная проверка

### Проверить все порты:
```bash
lsof -i :3000 -i :4000 -i :4200
```

**Ожидается:** Все три порта заняты

### Проверить health checks:
```bash
# Backend
curl http://localhost:4000/api/health
# → {"ok":true}

# React
curl http://localhost:4200
# → HTML страница

# Landing
curl http://localhost:3000
# → HTML страница
```

### Проверить WebApp:
```bash
# Открыть в браузере
open http://localhost:4000/webapp/admin
```

---

## 🛑 Остановка всех сервисов

```bash
./stop-all.sh
```

Или вручную:
```bash
# Найти процессы
ps aux | grep -E "tsx watch|vite|next dev"

# Остановить
pkill -f "tsx watch.*server.ts"  # Backend
pkill -f "vite.*4200"             # React
pkill -f "next dev.*3000"          # Landing
```

---

## 🔐 Учетные данные для входа

- **Email:** `admin@system.com`
- **Password:** `admin123`
- **Role:** `SUPER_ADMIN`

---

## 📝 Примечания

- **База данных:** SQLite (не нужен Docker)
- **Backend:** Автоматически отдает React build для WebApp
- **WebApp:** Использует same-origin для избежания CORS
- **Все сервисы:** Hot reload включен

---

**Готово! Все сервисы должны работать! 🎉**

